{
  "version": "0.2.0",
  "configurations": [
    {
        "name": "Uvicorn",
        "type": "python",
        "request": "launch",
        "module": "uvicorn",
        "args": ["echoline.main:create_app", "--factory", "--host", "0.0.0.0", "--reload"]
    }
  ]
}
